🚀 Syncline MSP — Git + Vercel Deployment Command Pack
Use this anytime you want to push updates to your production website.
🧩 1. Navigate to your project folder
Code
cd C:\Temp\shakil-it-solutions_BACKUP_ONLY_Test_PUSH
🧩 2. Confirm GitHub remote (should point to Syncline-MSP)
Code
git remote -v
Expected:

Code
origin  https://github.com/msausha/Syncline-MSP.git
🧩 3. Make sure you’re logged into the correct Vercel account
Code
vercel logout
vercel login
Choose the account:

Code
mshakilau-5254
🧩 4. Link the folder to the correct Vercel project (only needed once per folder)
Code
vercel link
Choose:

Yes (set up project)

mshakilau-5254’s projects

Yes (link existing)

syncline-main-site

Y (pull env vars)

🧩 5. Pull latest production code BEFORE pushing
This prevents conflicts and keeps your local folder synced.

Code
git pull origin main
If conflicts appear, fix them → then:

Code
git add <file>
git commit -m "Resolve merge conflict"
🧩 6. Add your new or updated files
Example:

Code
git add .
or specific:

Code
git add api/contact.js
🧩 7. Commit your changes
Code
git commit -m "Describe your update here"
🧩 8. Push to GitHub (this triggers Vercel deployment)
Code
git push origin main
If Git rejects the push:

Code
git pull origin main
Fix conflicts → commit → push again.

🧩 9. Test your live API (optional but recommended)
Code
curl -X POST https://syncline.com.au/api/contact -H "Content-Type: application/json" -d "{\"name\":\"Test\",\"email\":\"test@example.com\",\"message\":\"Hello from Syncline\"}"
🧩 10. View deployment logs (optional)
Code
vercel logs syncline-main-site
🧩 11. Deploy manually (rarely needed)
Code
vercel --prod
🏆 This is your complete, production‑safe workflow
You now have:

GitHub remote alignment

Vercel account alignment

Pull → add → commit → push workflow

Conflict resolution

API testing

Deployment verification

This is the exact sequence used by professional MSPs and SaaS teams.